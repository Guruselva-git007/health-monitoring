apiVersion: apps/v1
kind: Deployment
metadata:
  name: health-monitoring-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: health-frontend
  template:
    metadata:
      labels:
        app: health-frontend
    spec:
      containers:
        - name: frontend
          image: your-registry/health-monitoring-frontend:latest
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: health-monitoring-frontend
spec:
  selector:
    app: health-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
